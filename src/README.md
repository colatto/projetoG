# `src`

Diretorio reservado.

A estrutura principal do projeto foi iniciada em `apps/` e `packages/` para separar frontend, backend, dominio e integracao. Se no futuro surgir necessidade de uma camada unica ou codigo transitorio de migracao, este diretorio pode ser reutilizado com uma decisao arquitetural explicita.
