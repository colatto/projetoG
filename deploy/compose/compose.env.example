# Copiar para compose.env (não commitar compose.env com credenciais).
# Tags devem coincidir com o build do GitHub Actions (deploy.yml → GHCR).

API_IMAGE=ghcr.io/YOUR_GITHUB_OWNER/YOUR_GITHUB_REPO-api:latest
WORKERS_IMAGE=ghcr.io/YOUR_GITHUB_OWNER/YOUR_GITHUB_REPO-workers:latest
